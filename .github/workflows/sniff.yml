name: Sniff
on:
  workflow_dispatch:

jobs:
  sniff:
    runs-on: ubuntu-latest
    steps:
      - name: Public sniff disabled
        run: echo "Use the protected Live shared desk workflow for an intentional one-off tick."
